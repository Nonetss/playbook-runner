---
import { CommandsPage } from "@/components/features/commands/components/commands-page"
import Layout from "@/layouts/Layout.astro"
import { getServerT } from "@/lib/i18n/server"

const { locale } = Astro.locals
const t = await getServerT(locale, "commands")
---

<Layout title={t("page.title")}>
  <CommandsPage client:only="react" />
</Layout>

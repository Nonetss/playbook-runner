**/node_modules
.git

**/dist
**/build
**/.next
**/.nuxt
**/.output
**/.svelte-kit
**/.astro
**/.turbo
.turbo

**/.wrangler
**/.alchemy
**/.expo
**/.vercel
*.log

Dockerfile
**/Dockerfile
docker-compose.yml

# Secrets stay out of image layers; runtime env comes from compose env_file,
# build-time public values come from compose build args
**/.env
**/.env.*
!**/.env.example

# Python caches / venvs / lockers
**/__pycache__
**/*.py[cod]
**/*.egg-info
**/.venv
**/.ruff_cache
.pyrightconfig.json

# Runtime state is bind-mounted at runtime, never baked into an image.
# It includes secrets, live inventory, environment, and runner artifacts.
.data/

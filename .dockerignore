**/node_modules
.git

**/dist
**/build
**/.next
**/.nuxt
**/.output
**/.svelte-kit
**/.astro
**/.turbo
.turbo

**/.wrangler
**/.alchemy
**/.expo
**/.vercel
*.log

Dockerfile
**/Dockerfile
docker-compose.yml

# Secrets stay out of image layers; runtime env comes from compose env_file,
# build-time public values come from compose build args
**/.env
**/.env.*
!**/.env.example

# Python caches / venvs / lockers
**/__pycache__
**/*.py[cod]
**/*.egg-info
**/.venv
**/.ruff_cache
.pyrightconfig.json

# apps/ansible runtime state — bind-mounted at runtime, never bake into image
# (playbook/ssh_key is a secret; inventory/env/artifacts are live host state)
apps/ansible/playbook/

---
description: Capture durable product context in PRODUCT.md
---

Run impeccable init.

**Input:** none required.

**Steps:**

1. Load `.agents/skills/impeccable/reference/init.md` and follow it.
2. Run the multi-round discovery interview if context is missing, then write `PRODUCT.md` (strategic: users, brand, principles).
3. Offer to write `DESIGN.md` (visual: colors, typography, components) only if real code already exists in `apps/frontend/`.
4. When done, recommend the next command (typically `/impeccable-shape` or `/impeccable-craft`).
